# Validation Report — Flask Pilot Annotation Run

Generated by actually executing a validation script
(`validate_flask_run.py`) against `queries.jsonl`,
`annotation_drafts.jsonl`, and `draft_relevance_judgments.jsonl`,
cross-checked against the real repository at
`C:\Projects\tara-rlcg\flask`, pinned commit
`6a2f545bfd8ed31e19066a299296917e034aca58`. Every number below is
computed output, not an asserted claim. Errors are reported, not
silently fixed, per this phase's explicit instruction — unlike the
prior FastAPI pilot run, no correction was required this time (see §3).

## Result: PASS. 0 validation errors. 2 queries flagged speculative (not an error — a disclosed finding).

## 1. Duplicate queries

- Duplicate `query_id`: **0** (20 unique IDs).
- Exact-duplicate query text: **0** (20 unique strings).
- Near-duplicate intent was not algorithmically checked (requires
  semantic judgment); manually cross-referenced — flask-002 and
  flask-017 intentionally share the same underlying subsystem
  (`copy_current_request_context`) under different category framings
  (Bug Fix vs. API Usage), which is expected and intentional, not a
  duplicate — see `human_annotation_checklist.md`'s ambiguity section.

## 2. Duplicate candidate files

- Within a single query's candidate list (across all four
  `annotation_drafts.jsonl` buckets): **0**.
- Duplicate `(query_id, file)` rows in `draft_relevance_judgments.jsonl`:
  **0** (62 rows, 62 unique pairs).

## 3. Invalid / broken paths

**0 broken paths** among 62 candidate-path references in
`annotation_drafts.jsonl` and 62 rows in
`draft_relevance_judgments.jsonl`. Every path was verified to exist on
disk at the pinned commit by the validation script. Unlike the prior
FastAPI pilot run, no correction was required in this pass.

## 4. Missing files

Not the same as "broken paths" (§3) — this checks whether a file this
run's own `potential_ambiguity`/`uncertainty` fields flagged as needed
but not found was actually located. Two such gaps are disclosed, not
silently left implicit:

- flask-004: no test file specifically matching "flash"/"flashing" was
  located by name; `tests/test_helpers.py` (confirmed to exist) was not
  individually searched for flash-specific test functions.
- flask-007: no documentation file was found discussing custom
  `SessionInterface` implementations.

Both are recorded in `human_annotation_checklist.md`'s "Add missing
files" section for human follow-up, not presented as resolved.

## 5. Empty fields

**0** — every row in `queries.jsonl` has a non-empty `query_id`,
`repository_id`, `category`, `difficulty`, and `query`.

## 6. Weak queries

Defined for this check as: every candidate across all four buckets is
`Low` confidence, or a query has zero candidates at all.

**0 weak queries.** Every one of the 20 queries has at least one
`Medium` or `High`-confidence candidate.

## 7. Speculative queries

Defined for this check as: the AI assistant's own `potential_ambiguity`
field contains an explicit "STRONG FLAG" marker, indicating the search
found no confirmed evidence the query's premise corresponds to an
actual, locatable defect/gap.

**2 speculative queries found:**

| Query | Category | Why flagged |
|---|---|---|
| flask-008 | feature_implementation | Not confirmed whether Flask itself implements any URL converters, or whether all converter implementations live entirely in Werkzeug (external dependency) — the feature request may be substantially or entirely out-of-repo. |
| flask-013 | testing | No evidence of an actual currently-inconsistent/flaky test was found — only the structural pattern (`pytest.importorskip("asgiref")`) that produces environment-dependent *skip* (not fail) behavior by design, which may be entirely correct, intended behavior rather than a bug. |

Both are carried into `human_annotation_checklist.md`'s "Resolve
ambiguity" section for explicit human disposition (accept as a
near-empty-relevance case, or replace).

## 8. Directory candidates

**0.** Every candidate path in this run resolves to a file, not a
directory — a difference from the prior FastAPI pilot run (which had
7 unresolved directory-level candidates). This reflects this run's
search style (individual test/doc files were named directly where
confirmed) rather than any difference in Flask's own directory
structure.

## 9. Category balance

| Category | Count | Required |
|---|---|---|
| bug_fix | 4 | 4 |
| feature_implementation | 4 | 4 |
| refactoring | 3 | 3 |
| testing | 3 | 3 |
| documentation | 2 | 2 |
| api_usage | 2 | 2 |
| code_search | 2 | 2 |

**Matches the required distribution exactly.**

## 10. Difficulty balance

| Difficulty | Count |
|---|---|
| easy | 7 |
| medium | 9 |
| hard | 4 |

No target distribution was specified for difficulty; reported
descriptively.

## 11. Average candidate count

Computed over `annotation_drafts.jsonl`'s four buckets combined:

**avg = 3.10, min = 1 (flask-019), max = 5 (flask-003, flask-006, flask-011)**

Lower than the prior FastAPI run's average (4.05) — see
`research_notes.md` §1 for a discussion of why (fewer directory-level
"padding" candidates, and this repository's smaller `docs_src/`
-equivalent surface, since Flask has no single directory that is
simultaneously documentation source and tested code the way FastAPI's
`docs_src/` is).

## 12. Summary

| Check | Result |
|---|---|
| Duplicate queries | PASS (0) |
| Duplicate candidate files | PASS (0) |
| Invalid/broken paths | PASS (0) |
| Missing files | 2 disclosed gaps, not hidden |
| Empty fields | PASS (0) |
| Weak queries | PASS (0) |
| Speculative queries | 2 found and disclosed (flask-008, flask-013) |
| Directory candidates | PASS (0) |
| Category balance | PASS (exact match) |
| Difficulty balance | Reported, no target specified |
| Average candidate count | 3.10 (min 1, max 5) |

This dataset is **not yet publication-ready** — it is a validated
*draft* awaiting human review per `human_annotation_checklist.md`,
with two queries requiring an explicit accept/replace decision before
grading proceeds (§7).
