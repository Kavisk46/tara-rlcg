# Validation Report — FastAPI Pilot Annotation Run

Generated by actually executing a validation script
(`validate_fastapi_run.py`) against `queries.jsonl`,
`annotation_drafts.jsonl`, and `draft_relevance_judgments.jsonl`, cross
-checked against the real repository at
`C:\Projects\tara-rlcg\fastapi`, pinned commit
`a375f6b948b99fa4260129856bbf11d037f363ef`. Every number below is
computed output, not an asserted claim.

## Result: PASS, with 1 corrected error and 7 open items requiring human resolution

## 1. Duplicate queries

- Duplicate `query_id` values: **none found** (20 unique IDs across 20 rows).
- Exact-duplicate query text: **none found** (20 unique query strings).
- Near-duplicate *intent* (e.g. fastapi-001/fastapi-016 both touch
  nested-model schema representation; fastapi-003/fastapi-015 both
  touch background-tasks+streaming) was **not** algorithmically
  checked — this requires semantic judgment. See §7's manual
  cross-reference below; none were judged to violate the "one query,
  one intent" rule, since each pair targets a different *category*
  (fix vs. documentation) over the same domain, which is expected and
  intentional (see `ANNOTATION_HANDBOOK.md` §2).

## 2. Duplicate candidate files within a single query

**None found.** No query's candidate list (across all four buckets:
primary, secondary, regression tests, documentation examples)
contains the same file path more than once.

## 3. Broken paths

**One broken path was found and corrected during this validation
pass**, not silently left in the delivered files:

- `fastapi-004`'s regression-test candidate was originally written as
  `tests/test_path_params_numeric_validations` (missing the
  `test_tutorial/` path segment). The validation script confirmed this
  path does not exist on disk. **Corrected** to
  `tests/test_tutorial/test_path_params_numeric_validations/test_tutorial001.py`,
  verified to exist, in both `annotation_drafts.jsonl` and
  `draft_relevance_judgments.jsonl` before this report was finalized.

After correction: **0 broken paths** remain among 81 candidate-path
references in `annotation_drafts.jsonl` and 74 path keys in
`draft_relevance_judgments.jsonl`'s `relevance_grades` objects.

## 4. Unresolved directory-level candidates

7 candidate-path references in `annotation_drafts.jsonl` point at a
**directory**, not a specific file — left unresolved deliberately
(guessing a specific filename inside an unread directory would risk
fabrication) rather than silently narrowed to one arbitrary file.
**None of these appear in `draft_relevance_judgments.jsonl`'s
`relevance_grades`** (which requires concrete file paths per
`RELEVANCE_ANNOTATION_HANDBOOK.md`'s validation rules) — they are
flagged in that file's `notes` field instead, for the human annotator
to resolve per `human_annotation_checklist.md` §1:

| Query | Directory |
|---|---|
| fastapi-003 | `tests/test_tutorial/test_background_tasks` |
| fastapi-012 | `tests/test_tutorial/test_request_forms` |
| fastapi-012 | `tests/test_tutorial/test_request_forms_and_files` |
| fastapi-012 | `tests/test_tutorial/test_request_form_models` |
| fastapi-014 | `fastapi/security/` |
| fastapi-015 | `docs/en/docs/` |
| fastapi-016 | `docs/en/docs/` |

## 5. Empty fields

**None found.** Every row in `queries.jsonl` has a non-empty
`query_id`, `repository_id`, `category`, `difficulty`, and `query`.

## 6. `commit_sha` consistency

**None found to be wrong or missing.** Every row in
`annotation_drafts.jsonl` and `draft_relevance_judgments.jsonl` carries
exactly `a375f6b948b99fa4260129856bbf11d037f363ef`, matching the pinned
commit confirmed via `git rev-parse HEAD` at the start of this run.

## 7. `query_id` referential consistency

**No mismatches.** Every `query_id` in `queries.jsonl` has exactly one
corresponding row in `annotation_drafts.jsonl` and exactly one in
`draft_relevance_judgments.jsonl`; no orphaned rows exist in either
direction, in either file.

## 8. Category balance

| Category | Count | Required |
|---|---|---|
| bug_fix | 4 | 4 |
| feature_implementation | 4 | 4 |
| refactoring | 3 | 3 |
| testing | 3 | 3 |
| documentation | 2 | 2 |
| api_usage | 2 | 2 |
| code_search | 2 | 2 |

**Matches the required distribution exactly.**

## 9. Difficulty balance

| Difficulty | Count |
|---|---|
| easy | 6 |
| medium | 9 |
| hard | 5 |

No target distribution was specified for difficulty in this task, so
this is reported descriptively, not checked against a requirement.

## 10. Candidate distribution per query

Computed over `annotation_drafts.jsonl`'s four candidate buckets
combined, per query:

| Query | Candidate count |
|---|---|
| fastapi-001 | 7 |
| fastapi-005 | 6 |
| fastapi-007 | 6 |
| fastapi-011 | 6 |
| fastapi-003 | 5 |
| fastapi-004 | 5 |
| fastapi-006 | 5 |
| fastapi-008 | 5 |
| fastapi-009 | 5 |
| fastapi-012 | 5 |
| fastapi-002 | 4 |
| fastapi-010 | 3 |
| fastapi-015 | 3 |
| fastapi-016 | 3 |
| fastapi-017 | 3 |
| fastapi-018 | 3 |
| fastapi-013 | 2 |
| fastapi-014 | 2 |
| fastapi-020 | 2 |
| fastapi-019 | 1 |

**avg = 4.05, min = 1 (fastapi-019), max = 7 (fastapi-001)**

The low end (fastapi-019, 1 candidate; fastapi-013/fastapi-014/
fastapi-020, 2 candidates) is not itself an error: fastapi-019 is a
Code Search query with a single, high-confidence answer by design (see
`annotation_drafts.jsonl`'s own `overall_uncertainty` for that row);
fastapi-013 and fastapi-014 are the two queries explicitly flagged as
weakly grounded (see `research_notes.md` §3) rather than padded with
speculative candidates to hit a higher count.

## 11. Cross-query file reuse (informational, not an error)

15 files/paths were suggested by more than one query — expected and
healthy for a repository of this structure (e.g. `fastapi/routing.py`
and `fastapi/dependencies/utils.py`, each appearing across 5 different
queries, are genuinely central to request handling). Full table in
`dataset_statistics.md` §4.

## 12. Summary

| Check | Result |
|---|---|
| Duplicate query_id | PASS (0) |
| Duplicate query text | PASS (0) |
| Duplicate candidate file within a query | PASS (0) |
| Broken paths | PASS after 1 correction (0 remaining) |
| Unresolved directory-level candidates | 7 open items, flagged not hidden |
| Empty required fields | PASS (0) |
| commit_sha consistency | PASS (0 mismatches) |
| query_id referential consistency | PASS (0 orphans) |
| Category balance | PASS (exact match) |
| Difficulty balance | Reported, no target specified |
| Candidate distribution | avg 4.05, min 1, max 7 -- both extremes explained above |

This dataset is **not yet publication-ready** — it is a validated
*draft* awaiting human review per `human_annotation_checklist.md`. The
one correction made during this pass (§3) demonstrates the validation
step catching a real, if minor, defect rather than being a formality.
