# Validation Report — Requests Pilot Annotation Run

Generated by actually executing a validation script
(`validate_requests_run.py`) against `queries.jsonl`,
`annotation_drafts.jsonl`, and `draft_relevance_judgments.jsonl`,
cross-checked against the real repository at
`C:\Projects\tara-rlcg\requests`, pinned commit
`1f6589ec3a1ee910f9a65cc3ceac60b26677bc0e`. Every number below is
computed output, not an asserted claim. Errors are reported, not
silently fixed, per this phase's explicit instruction.

## Result: PASS. 0 validation errors. 2 queries flagged speculative (disclosed findings, not errors).

## 1. Schema conformance

`draft_relevance_judgments.jsonl` was checked, row by row, for exactly
the five required flat keys (`query_id`, `repository`, `file`, `grade`,
`reason`) with no extras and no nested (`dict`/`list`) values.

**0 schema violations** across 47 rows. No `commit_sha` or any other
extra field was added this run (unlike the prior Flask run's
`draft_relevance_judgments.jsonl`, which included `commit_sha` — that
run's task did not specify an exact field set the way this run's does;
this run's explicit 5-field schema was followed exactly, with no
drift).

## 2. Duplicate queries

- Duplicate `query_id`: **0** (20 unique IDs).
- Exact-duplicate query text: **0** (20 unique strings).
- Near-duplicate intent was manually cross-referenced (not
  algorithmic): `requests-001`/`requests-010`/`requests-014`
  intentionally share the same adapter-selection subsystem under three
  category framings (Bug Fix/Refactoring/Testing) — expected and
  intentional, not a duplicate, per this project's established
  convention (see the equivalent finding in both prior pilot runs).

## 3. Duplicate candidate files

- Within a single query's candidate list (`annotation_drafts.jsonl`):
  **0**.
- Duplicate `(query_id, file)` rows in `draft_relevance_judgments.jsonl`:
  **0** (47 rows, 47 unique pairs).

## 4. Invalid paths

**0 broken paths** among 47 candidate-path references in
`annotation_drafts.jsonl` and 47 rows in
`draft_relevance_judgments.jsonl`. Every path was verified to exist on
disk at the pinned commit by the validation script.

## 5. Missing files

Disclosed gaps (not "broken," but flagged as searches that did not
reach a definitive answer):

- `requests-003`/`requests-006`: no digest-auth-specific or
  auth-class-specific test coverage was individually located within
  `tests/test_requests.py` (3,094 lines, not exhaustively searched).
- `requests-002`: the exact line(s) where response cookies are merged
  into `session.cookies` were not individually located.

Both are recorded in `human_annotation_checklist.md`'s "Add missing
files" section.

## 6. Weak queries

Defined as: every candidate is `Low` confidence, or zero candidates.

**0 weak queries.** Every query has at least one `Medium`/`High`
-confidence candidate.

## 7. Speculative queries

Defined as: `annotation_drafts.jsonl`'s own `ambiguity_notes` field
contains an explicit "STRONG FLAG" marker.

**2 speculative queries found:**

| Query | Category | Why flagged |
|---|---|---|
| requests-008 | feature_implementation | The existing, documented `verify=` parameter already allows passing a custom CA bundle path per-request/per-session — this query may already be substantially addressed by existing functionality rather than describing a true gap. |
| requests-013 | testing | No evidence of an actual currently-flaky low-level/socket test was found — only the general observation that real-socket tests are inherently more flakiness-prone than mocked ones. |

## 8. Directory candidates

**0.** Every candidate path resolves to a file.

## 9. Category imbalance

| Category | Count | Required |
|---|---|---|
| bug_fix | 4 | 4 |
| feature_implementation | 4 | 4 |
| refactoring | 3 | 3 |
| testing | 3 | 3 |
| documentation | 2 | 2 |
| api_usage | 2 | 2 |
| code_search | 2 | 2 |

**No imbalance — matches the required distribution exactly.**

## 10. Difficulty imbalance

| Difficulty | Count |
|---|---|
| medium | 12 |
| easy | 5 |
| hard | 3 |

No target distribution was specified for difficulty. **Noted as
imbalanced relative to the two prior pilot runs**: this run skews more
heavily toward `medium` (60% of queries) than either FastAPI's (45%
medium) or Flask's (45% medium) runs. This reflects Requests' smaller,
more uniformly-scoped codebase (6,394 LOC across 18 files, vs.
FastAPI's ~14,900 across 48 and Flask's ~9,500 across 24) — fewer
queries naturally fell at the "single-file, trivial" (easy) or
"deeply cross-cutting, many-file" (hard) extremes. Reported, not
corrected, per this phase's "never silently fix" instruction; a future
round could deliberately rebalance if a flatter distribution is
desired.

## 11. Schema inconsistencies

Checked directly:

- `queries.jsonl`: exactly matches the 6-field schema specified
  (`query_id`, `repository_id`, `category`, `difficulty`, `query`,
  `notes`) for all 20 rows.
- `draft_relevance_judgments.jsonl`: exactly matches the 5-field flat
  schema specified for all 47 rows (§1).
- `annotation_drafts.jsonl`: uses the richer, non-flat schema this
  phase's own instructions require (`primary_candidates`,
  `secondary_candidates`, `regression_tests`, `documentation_examples`,
  `ambiguity_notes`, each candidate a nested object) — this is by
  design (Phase 4 does not specify a flat schema the way Phase 5
  explicitly does), not an inconsistency.

**0 schema inconsistencies found.**

## 12. Summary

| Check | Result |
|---|---|
| Duplicate queries | PASS (0) |
| Duplicate candidate files | PASS (0) |
| Invalid paths | PASS (0) |
| Missing files | 3 disclosed gaps, not hidden |
| Weak queries | PASS (0) |
| Speculative queries | 2 found and disclosed (requests-008, requests-013) |
| Directory candidates | PASS (0) |
| Category imbalance | PASS (exact match) |
| Difficulty imbalance | Noted (medium-heavy), not an error |
| Schema inconsistencies | PASS (0) |

This dataset is **not yet publication-ready** — it is a validated
*draft* awaiting human review per `human_annotation_checklist.md`,
with two queries requiring an explicit accept/replace decision before
grading proceeds (§7).
