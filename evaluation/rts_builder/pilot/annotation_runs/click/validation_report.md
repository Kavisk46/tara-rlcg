# Validation Report — Click Pilot Annotation Run

Generated by actually executing a validation script
(`validate_click_run.py`) against `queries.jsonl`,
`annotation_drafts.jsonl`, and `draft_relevance_judgments.jsonl`,
cross-checked against the real repository at
`C:\Projects\tara-rlcg\click`, pinned commit
`00e592cea702e0b2caa0dee42489fdb1c22cd845`. Every number below is
computed output, not an asserted claim. Errors are reported, not
silently fixed.

## Result: PASS. 0 validation errors. 1 query flagged speculative (disclosed finding, not an error).

## 1. Schema conformance

`draft_relevance_judgments.jsonl` checked row by row for exactly the
five required flat keys (`query_id`, `repository`, `file`, `grade`,
`reason`) with no extras and no nested values.

**0 schema violations** across 54 rows.

## 2. Duplicate queries

- Duplicate `query_id`: **0** (20 unique IDs).
- Exact-duplicate query text: **0** (20 unique strings).
- Near-duplicate intent, manually cross-referenced: `click-001`/
  `click-011`/`click-012` all touch shell completion under three
  category framings (Bug Fix/Refactoring/Testing); `click-004`/
  `click-017` both touch parent-context access under two framings
  (Bug Fix/API Usage) — both patterns are intentional and consistent
  with the same convention observed in all three prior pilot runs, not
  duplicates.

## 3. Duplicate candidate files

- Within a single query's candidate list (`annotation_drafts.jsonl`):
  **0**.
- Duplicate `(query_id, file)` rows in `draft_relevance_judgments.jsonl`:
  **0** (54 rows, 54 unique pairs).

## 4. Invalid paths

**0 broken paths** among 54 candidate-path references in
`annotation_drafts.jsonl` and 54 rows in
`draft_relevance_judgments.jsonl`. One path was corrected *during*
Phase 3/4 rather than left broken: `click-008`'s search initially
surfaced `examples/aliases` (a directory); before finalizing, this was
resolved to the concrete file `examples/aliases/aliases.py`, which was
then read directly and included as a High-confidence candidate. This
was a proactive resolution during drafting, not a defect caught after
the fact.

## 5. Missing files

Disclosed gaps (not "broken," but searches that did not reach a
definitive answer):

- `click-002`/`click-010`: specific help-record/help-formatting method
  names in `core.py`/`formatting.py` were not individually confirmed
  by name.
- `click-003`/`click-018`: `ParamType`'s exact validation-failure
  method name was not individually confirmed.
- `click-020`: the specific `Command`/`Parameter` methods calling into
  `parser.py`'s internals were not individually named.

All three recorded in `human_annotation_checklist.md`'s "Add missing
files" section.

## 6. Weak queries

Defined as: every candidate is `Low` confidence, or zero candidates.

**0 weak queries.** Every query has at least one `Medium`/`High`
-confidence candidate.

## 7. Speculative queries

Defined as: `annotation_drafts.jsonl`'s own `ambiguity_notes` field
contains an explicit "STRONG FLAG" marker.

**1 speculative query found** (fewer than any prior pilot run — FastAPI:
2, Flask: 2, Requests: 2):

| Query | Category | Why flagged |
|---|---|---|
| click-013 | testing | No evidence of an actual currently-inconsistent platform/terminal test was found — only the general observation that terminal/platform-sensitive code is inherently more prone to environment-dependent behavior, reinforced by the very recent Colorama-removal/Windows-ANSI change. |

## 8. Directory candidates

**0** in the final artifacts. One was found and resolved during
drafting (§4) rather than carried forward unresolved — a difference
from the FastAPI pilot run, which left 7 directory candidates
unresolved for the human annotator.

## 9. Category imbalance

| Category | Count | Required |
|---|---|---|
| bug_fix | 4 | 4 |
| feature_implementation | 4 | 4 |
| refactoring | 3 | 3 |
| testing | 3 | 3 |
| documentation | 2 | 2 |
| api_usage | 2 | 2 |
| code_search | 2 | 2 |

**No imbalance — matches the required distribution exactly.**

## 10. Difficulty imbalance

| Difficulty | Count |
|---|---|
| medium | 13 |
| easy | 5 |
| hard | 2 |

No target distribution was specified. **Noted as the most
medium-skewed of the four pilot runs to date** (65% medium, vs.
Requests' 60%, and FastAPI/Flask's 45% each) — reported descriptively,
not corrected. A plausible contributor: `core.py`'s unusual size
(3,792 lines, ~30% of the package) concentrates many distinct concerns
in one file, which tended to produce "touches one large, non-trivial
file" (medium) queries rather than either single-tiny-file (easy) or
many-files-across-the-package (hard) extremes.

## 11. Schema inconsistencies

Checked directly:

- `queries.jsonl`: exactly matches the 6-field schema for all 20 rows.
- `draft_relevance_judgments.jsonl`: exactly matches the 5-field flat
  schema for all 54 rows (§1).
- `annotation_drafts.jsonl`: uses the richer, non-flat schema this
  phase's own instructions require — by design, not an inconsistency.

**0 schema inconsistencies found.**

## 12. Summary

| Check | Result |
|---|---|
| Duplicate queries | PASS (0) |
| Duplicate candidate files | PASS (0) |
| Invalid paths | PASS (0) |
| Missing files | 3 disclosed gaps, not hidden |
| Weak queries | PASS (0) |
| Speculative queries | 1 found and disclosed (click-013) |
| Directory candidates | PASS (0 in final artifacts; 1 resolved during drafting) |
| Category imbalance | PASS (exact match) |
| Difficulty imbalance | Noted (most medium-heavy run to date), not an error |
| Schema inconsistencies | PASS (0) |

This dataset is **not yet publication-ready** — it is a validated
*draft* awaiting human review per `human_annotation_checklist.md`,
with one query (`click-013`) requiring an explicit accept/replace
decision before grading proceeds (§7).
