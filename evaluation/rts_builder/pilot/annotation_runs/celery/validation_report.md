# Validation Report — Celery Pilot Annotation Run

Generated by actually executing a validation script
(`validate_celery_run.py`) against `queries.jsonl`,
`annotation_drafts.jsonl`, and `draft_relevance_judgments.jsonl`,
cross-checked against the real repository at
`C:\Projects\tara-rlcg\celery`, pinned commit
`f109abf852525b69a1b6eee0457c6cd5561e0529`. Every number below is
computed output, not an asserted claim. Errors are reported, not
silently fixed.

## Result: PASS, with 1 corrected duplicate and 1 query (celery-013) whose primary candidate was found only after a follow-up search performed during the Phase 11 audit (both disclosed, not hidden).

## 1. Schema conformance

`draft_relevance_judgments.jsonl` checked row by row for exactly the
five required flat keys (`query_id`, `repository`, `file`, `grade`,
`reason`) with no extras and no nested values.

**0 schema violations** across 50 rows.

## 2. Duplicate queries

- Duplicate `query_id`: **0** (20 unique IDs).
- Exact-duplicate query text: **0** (20 unique strings).
- Near-duplicate intent, manually cross-referenced: `celery-001`/
  `celery-006`/`celery-010` all touch `canvas.py`'s chain machinery
  under three category framings (Bug Fix/Feature/Refactoring);
  `celery-008`/`celery-015` both touch `StampingVisitor` under two
  framings (Feature/Documentation); `celery-014`/`celery-017` both
  touch task retry under two framings (Testing/API Usage) — all
  intentional and consistent with the convention observed in every
  prior pilot run, not duplicates.

## 3. Duplicate candidate files

**One real duplicate was found and corrected during this validation
pass, not silently left in the delivered files:**

- `celery-020`'s draft originally listed `celery/schedules.py` twice —
  once as a primary candidate (for `crontab_parser`) and once as a
  secondary candidate (for `crontab`). The validation script correctly
  flagged this as a within-query duplicate candidate file. **Corrected**
  by merging into a single entry (a relevance grade applies to the
  file, not to an individual class within it) in
  `annotation_drafts.jsonl` before this report was finalized.

After correction: **0 duplicate candidate files** within any query's
`annotation_drafts.jsonl` list, and **0** duplicate `(query_id, file)`
rows among 50 rows in `draft_relevance_judgments.jsonl`.

## 4. Invalid paths

**0 broken paths** among 52 candidate-path references in
`annotation_drafts.jsonl` (post-correction) and 50 rows in
`draft_relevance_judgments.jsonl`. Every path was verified to exist on
disk at the pinned commit.

## 5. Weak queries

Defined as: every candidate is `Low` confidence, or zero candidates
entirely.

**0 weak queries** by this definition. A related but distinct
condition was also checked: whether a query has zero *primary*
candidates (regardless of secondary-candidate confidence). **This
initially caught `celery-013`**: its first-draft sole candidate
(`pyproject.toml`'s `flaky` marker declaration) was a
`secondary_candidates` entry only, confirming the marker mechanism's
existence but not pointing at any specific flaky test. **This was
resolved during the Phase 11 audit** (see `README.md`) by searching
`t/` directly for `flaky` usage, which located the real mechanism
(`t/integration/conftest.py`) and its extensive application
(`t/integration/test_canvas.py`, 10+ uses). After this correction,
**0 queries lack a primary candidate.**

## 6. Speculative queries

Defined as: `annotation_drafts.jsonl`'s own `ambiguity_notes` field
contains an explicit "STRONG FLAG" marker (checked programmatically).

**0 speculative queries remain** after the Phase 11 resolution
described in §5. `celery-013` was the run's only candidate for this
status and is now resolved with concrete primary candidates — see
`README.md`'s Phase 11 audit for the full account. This is the first
of the five pilot runs to date to reach zero speculative queries
(FastAPI: 2; Flask: 2; Requests: 2; Click: 1; Celery: 0) — attributable
to the audit-stage follow-up search, not to this run's initial draft
being inherently more thorough than its predecessors.

## 7. Directory candidates

**2 found in `annotation_drafts.jsonl`, correctly excluded from
`draft_relevance_judgments.jsonl`:**

| Query | Directory |
|---|---|
| celery-004 | `t/unit/worker` |
| celery-005 | `docs/getting-started/backends-and-brokers` |

Both are flagged in `human_annotation_checklist.md` for resolution to
specific files — neither was guessed at or silently narrowed to an
arbitrary file within the directory.

## 8. Category balance

| Category | Count | Required |
|---|---|---|
| bug_fix | 4 | 4 |
| feature_implementation | 4 | 4 |
| refactoring | 3 | 3 |
| testing | 3 | 3 |
| documentation | 2 | 2 |
| api_usage | 2 | 2 |
| code_search | 2 | 2 |

**Matches the required distribution exactly.**

## 9. Difficulty balance

| Difficulty | Count |
|---|---|
| medium | 13 |
| easy | 4 |
| hard | 3 |

No target distribution was specified. 65% medium, tying with the Click
pilot run for the most medium-skewed of the five runs to date —
plausibly attributable to Celery's own large, subsystem-concentrated
files (`canvas.py` at 2,443 lines, `app/base.py` at 1,635,
`backends/base.py` at 1,284) producing the same "touches one large,
non-trivial file" pattern observed for Click's `core.py`.

## 10. Schema consistency

Checked directly:

- `queries.jsonl`: exactly matches the 6-field schema for all 20 rows.
- `draft_relevance_judgments.jsonl`: exactly matches the 5-field flat
  schema for all 50 rows (§1).
- `annotation_drafts.jsonl`: uses the richer, non-flat schema this
  phase's own instructions require — by design, not an inconsistency.

**0 schema inconsistencies found.**

## 11. Summary

| Check | Result |
|---|---|
| Duplicate queries | PASS (0) |
| Duplicate candidate files | 1 found and corrected during this pass (0 remaining) |
| Invalid paths | PASS (0) |
| Weak queries (confidence-based) | PASS (0) |
| Queries with no primary candidate | 1 found during drafting, resolved during Phase 11 audit (0 remaining) |
| Speculative queries | 1 found during drafting, resolved during Phase 11 audit (0 remaining) |
| Directory candidates | 2 found, correctly excluded from final relevance file, flagged for human resolution |
| Category balance | PASS (exact match) |
| Difficulty balance | Noted (medium-heavy, tied with Click), not an error |
| Schema consistency | PASS (0) |

This dataset is **not yet publication-ready** — it is a validated
*draft* awaiting human review per `human_annotation_checklist.md`.
Unlike the state at the end of Phase 7, every query now has at least
one primary candidate following the Phase 11 audit's resolution of
`celery-013` (see `README.md`).
